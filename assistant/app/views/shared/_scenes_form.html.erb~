<%= form_for(@scene) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :text, placeholder: "Numero" %>
  </div>
  <div class="field">
    <%= f.text_area :lieu, placeholder: "Titre" %>
  </div>
  <div class="field">
    <%= f.text_area :instant, placeholder: "Titre" %>
  </div>
  <%= f.submit "Post", class: "btn btn-large btn-primary" %>
<% end %>
